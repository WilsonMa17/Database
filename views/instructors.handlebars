<html>

<div id="selectText"><a>To enroll a new instructor, fill out the following form:</a></div>
<form id="addinstructor" action="/instructors" method= "post">
		<div class="enrollFormField">
			<label for="fname">First name:</label>
  			<input type="text" id="firstName" name="firstName" value="">
  		</div>
  		<div class="enrollFormField">
  			<label for="lname">Last name:</label>
  			<input type="text" id="lastName" name="lastName" value="">
  		</div>
      <div class="enrollFormField">
        <label for="address">Address:</label>
        <input type="text" id="address" name="address" placeholder="Address, City, State, Zip">
      </div>
      <div class="enrollFormField">
        <label for="phone">Phone Number:</label>
        <input type="tel" id="phoneNumber" name="phoneNumber" placeholder="555-555-5555">
      </div>

  		<div class="enrollFormField">
  			<label for="DOB">Date of Birth:</label>
  			<input type="Date" id="DOB" name="DOB" value=" ">
  		</div>
  		
  		<div class="enrollFormField">
  			<label for="instrumentTaught">Instrument Taught:</label>
  			<input type="text" id="instrumentTaught" name="instrumentTaught" value="">
  		</div>
  		<div class="enrollFormField">
  			<input type="submit" value="Enroll">
  		</div>
</form>
<br>

<div id="selectText"><a>If you wish to update an instructor's info, or remove an instructor, select the instructor you wish to update or remove and click Update or Remove</a></div>

<table class="allTables" id="instructorTable">
        <thead>
          
            <th>ID</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Address</th>
            <th>Phone</th>
            <th>Date Of Birth</th>
            <th>Instruments Taught</th>
         
        </thead>
        <tbody id="instructorBody">
        	{{#each instructors}}
          <tr>
            <td>{{instructorID}}</td>
            <td>{{firstName}}</td>
            <td>{{lastName}}</td>
            <td>{{address}}</td>
            <td>{{phoneNumber}}</td>
            <td>{{DOB}}</td>
            <td>{{instrumentTaught}}</td>
            <td><button><a href="/instructors/{{instructorID}}">Update</a></button></td>
            <td><button onclick="deleteinstructor({{instructorID}})">Delete</button></td>
          </tr>
          {{/each}}
          
        </tbody>
      </table>

<br/>
</html>